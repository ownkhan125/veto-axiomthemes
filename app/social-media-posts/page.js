import SocialPostsClient from './SocialPostsClient';

export const metadata = {
  title: 'Social Posts — Veto · Reyna for Nevada',
  description:
    'The campaign content library — feed posts and stories from the Veto social design system. Every creative previewed in full, with no cropping or stretching.',
};

export default function SocialPostsPage() {
  return <SocialPostsClient />;
}
