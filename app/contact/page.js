import ContactClient from './ContactClient';

export const metadata = {
  title: 'Contact — Veto · Reyna for Nevada',
  description:
    'Reach the campaign directly. Press inquiries, volunteer offers, endorsement requests, or general questions — every message routed within one business day.',
};

export default function ContactPage() {
  return <ContactClient />;
}
