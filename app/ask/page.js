import AskClient from './AskClient';

export const metadata = {
  title: 'Ask a Question — Veto · Reyna for Nevada',
  description:
    'Submit a public question and we answer on the record within seven days. Read past answers from across the district.',
};

export default function AskPage() {
  return <AskClient />;
}
