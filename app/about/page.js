import AboutClient from './AboutClient';

export const metadata = {
  title: 'About — Veto · Reyna for Nevada',
  description:
    'Third-generation Nevadan. Former public defender. Small-business owner. The story behind the campaign.',
};

export default function AboutPage() {
  return <AboutClient />;
}
