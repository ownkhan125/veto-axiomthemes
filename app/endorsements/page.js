import EndorsementsClient from './EndorsementsClient';

export const metadata = {
  title: 'Endorsements — Veto · Reyna for Nevada',
  description:
    'Backed by labor councils, editorial boards, elected officials, and civic groups across Nevada. Updated weekly.',
};

export default function EndorsementsPage() {
  return <EndorsementsClient />;
}
