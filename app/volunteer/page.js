import VolunteerClient from './VolunteerClient';

export const metadata = {
  title: 'Volunteer — Veto · Reyna for Nevada',
  description:
    'Join the movement. Doors, phones, events, translation — pick a lane and a regional lead reaches you within 48 hours.',
};

export default function VolunteerPage() {
  return <VolunteerClient />;
}
